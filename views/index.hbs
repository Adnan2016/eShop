<!--
<div class="row">
    <h1> {{title}} </h1>
    <div class="col-sm-6 col-md-4 col-md-offset-4 col-sm-offset-3">
        <div id="success" class="alert alert-success"{{#if noMessage}} hidden{{/if}}>
            {{successMsg}}
        </div>
    </div>
</div>
{{# each products}}
    <div class="row">
        {{#each this }}
            <div class="col-sm-6 col-md-4">
                <div class="thumbnail">
                    <img src="{{ this.image }}" class="img-responsive">
                    <div class="caption">
                        <h3>{{this.name}}</h3>
                        <p class="category">{{this.category}}</p>


                        <div class="clearfix">
                            <div class="price pull-left">€ {{this.price}}</div>
                            <a href="/shop/{{this._id}}" class="btn btn-primary pull-right" role="button">Shop</a>

                        </div>


                    </div>
                </div>
            </div>
        {{/each}}

    </div> &lt;!&ndash; Row End &ndash;&gt;
{{/each}}-->
<div class="container">
    <div class="row">
        {{#each products}}
            <div class="col-sm-6 col-md-4">
                <a href="/product/{{this._id}}">
                    <div class="thumbnail">
                        <img src="{{ this.image }}" class="img-responsive">
                    </div>
                    <div class="caption">
                        <h3>{{this.name}}</h3>
                        <p>{{this.category.name}}</p>
                        <p> € {{this.price}} </p>
                        <a href="/shop/{{this._id}}" class="btn btn-primary pull-right" role="button">Buy</a>
                    </div>

                </a>
            </div>
        {{/each}}
    </div>
</div>

